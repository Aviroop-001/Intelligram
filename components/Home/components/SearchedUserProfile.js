import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const SearchedUserProfile = ({otherUser}) => {
  return (
    <View>
      <Text>SearchedUserProfile</Text>
    </View>
  )
}

export default SearchedUserProfile

const styles = StyleSheet.create({})